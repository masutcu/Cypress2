{
  "name": "cypress2023",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run_day01": "npx cypress run --spec 'cypress/e2e/day01/' --browser chrome",
    "mysmoketest": "npx cypress run --spec 'cypress/e2e/day02/' --browser edge --headed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.5",
    "@faker-js/faker": "^8.2.0",
    "cypress": "^13.3.3",
    "cypress-file-upload": "^5.0.8",
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1"
  }
}
